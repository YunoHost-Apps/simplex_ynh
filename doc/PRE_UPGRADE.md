We are now using a domain name for Simplex instead of the server IP address. In order to upgrade, you will need to manually define the Simplex domain.

`yunohost app setting simplex domain -v <smp.domain.tld>`